apiVersion: v1
kind: Service
metadata:
  name: {{ include "integration-chart.fullname" . }}
  labels:
    {{- include "integration-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "integration-chart.selectorLabels" . | nindent 4 }}
